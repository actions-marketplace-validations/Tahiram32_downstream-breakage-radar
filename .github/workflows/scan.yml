name: Breakage Radar

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Run breakage radar action
        uses: ./
        with:
          base-ref: origin/main
          format: text
          fail-on: none
